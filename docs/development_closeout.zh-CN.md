# 开发收口与维护范围

2026-09-12。本轮完成已有工具交付与已确认问题修复，随后停止主动扩展开发。
研究状态见[研究索引](../analysis/README.md)，既有运行入口继续可用。

## 已交付与本次修复

R1/R1c、R2/R3、R4 前缀与 DUAL 的既定阶段已完成，保留各报告中的样本、结论和局限。
大实例阶段未启动，不通过追加实验改变原停止决定。CPU/CUDA 成果与三批统计拆分已经交付。

[prepare](prepare_review.zh-CN.md) 已随 PR #60 合并；
[本地审查工具](local_review.zh-CN.md)已随
[PR #61](https://github.com/Mikoto-19909/greedy-failure-structures/pull/61)
合并为 `f4756aa9e746708e44b91b2371efd9280adf28ad`，合并后完整测试与复现矩阵通过。
[实施记录](local_review_plan.zh-CN.md)保留真实模型调用、替代进程、Windows full
及 Linux 原生回归各自的验收范围，不将静态无发现解释为正确性证明。

本次带入原 `e5c6fed` 的 R2 局部修复：检查点或工作进程已经失败时，后端日志写入失败
作为原异常附注保留，不再替换主要错误；完成事件日志失败仍会中止运行。
保持保存顺序、检查点复用、后端选择、预算和资源回收策略；不提取结果循环或绘图模块。
具体操作见[R2 使用说明](../analysis/r2_usage.zh-CN.md)与[后端说明](r2_production_backends.zh-CN.md)。

## 验收范围

在包含 PR #61 的 main 基线上整合，15 项 R2 与生产后端相关测试通过。
独立代理另实际验证默认 Python 三图、真实 Numba 两图的新跑与完成态续跑，以及
write/close × 检查点/工作进程/完成日志失败六场景。原异常、日志附注、失败状态、
保存字节与恢复计算数量符合预期；损坏最优值、检查点种子和输入设计种子均被拒绝。
审阅无遗留发现。故障矩阵以单工作进程和替代求解器注入错误，真实 Numba 另行运行；
不将其解释为真实磁盘硬件故障测试。当前整合版本的默认检查运行 512 项（180.772 秒），
507 项通过、5 项条件跳过，必需研究验证与 42 个源码文件的 mypy 通过。
跳过项为 Matplotlib、OR-Tools、两项真实 CUDA 和 POSIX venv 回归；本机 Python 3.12.14、
NumPy 2.3.5、Numba 0.67.0、SciPy 1.18.1、mypy 2.3.0 环境未安装或升级依赖。

本地日志保留于本次收口工作树的 `results/closeout-validation/`，独立探针及短记录位于
`results/independent-closeout-review/`，均不随源码发布。合并前核对当前提交的必需 CI，
合并后核对主干检查；未执行或条件跳过的检查不记为通过。

本次不修改算法、公共记录、CSV、种子、实例身份、验证内核、检查系统或 GPU 实现。
既有 full 结果只支撑对应版本的工具验收；R2 整合使用当前版本的检查结果。
不重跑正式研究语料或硬件性能测量，不为收口新增文档措辞测试。

## 后续状态与停止条件

- `InstanceRecord` 同文件校验提取保留在候选分支 `codex/instance-family-validation`，
  本轮不合并；其他进一步结构拆分暂缓，历史计划不自动重新启动。
- 三次真实 PR 效果试用留待实际使用阶段；尚未完成，不宣称审查效率、发现率或误报率
  已经充分验证。这项评估不阻塞首版交付，也不要求继续开发新能力。
- 原工作树、本地结果、未跟踪研究文档/PDF 与冻结复现入口保留。清理不作为收口条件；
  未发布材料仍只在本地，不将保留目录描述成已备份。
- 历史 DUAL 继续使用固定快照恢复的原源码与证据，见[恢复说明](../analysis/r4_dual_usage.zh-CN.md)。
  当前维护源码不能冒用旧 `code_revision`，不缩小检查范围或绕过源码核对。

两项交付进入 main、适用验证与独立审阅完成、主干 CI 通过且本轮无已知未解决缺陷后，
本轮即结束。之后只按实际问题维护；新功能、额外重构、新研究实验和主动效果评估另定范围。
历史报告与本地证据不因开发结束而删除或重新生成。
