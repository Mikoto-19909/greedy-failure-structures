# 分支整合与剩余工作

本次记录对应 2026-09-23 的集中整合。目标是让主检出包含现有成果，移除其他工作目录，并同步本地与远端 `main`。
未完成的设计随成果保留，不在本次整合中追加功能或正式实验。

## 整合顺序与当前状态

先保存各目录的未提交材料，再以本地 `main` 的 `3de1200` 为代码基础。
申请材料分支和原面板分支保留真实合并关系，随后接入组件组合与教学页面。
现有开发分支已全部成为整合版本的祖先；整体验证、独立审查和固定入口使用核查均已通过。
五个附加注册工作树、一个已注销目录的残留，以及七个旧验收克隆已保存后移除。
本记录随整合请求进入 `main`；开发分支在合并后回收，以包含本记录的 `main` 作为继续工作的统一版本。

## 各分支成果与待办

- `codex/application-assets-plan-20260916`：已保存研究申请资产计划，清除了仓库中的个人申请材料，并修正历史说明；原提交为 `ac6cddb`。
  主目录另有两版可视化实验设计草图，本次一并纳入；正式配置服务和实验运行尚未接入，仍属待办。
  个人申请正文继续留在本地忽略目录，不随代码发布。
- `codex/dashboard-workbench-layer1-20260919`：原提交为 `73d01c4`，包含工作台、研究模块和本地索引三项实现。
  三项补丁已分别由 `2642ee8`、`dec5fd6`、`4a554f1` 进入本地 `main`；本次补齐原分支合并关系，避免留下平行开发副本。
- `codex/dashboard-main-integration-20260921`：`a9ef2d3` 已实现统一运行中心、研究分析和实验配置管理，已包含在本地 `main` 中。
  备份恢复功能与进一步规模优化曾明确暂缓，本次继续记录为待办，不扩展实现。
- `codex/component-composition-design-20260921`：原有未提交组件已保存为 `b761dcc` 并整合，包含比较计算、渲染、回放文档和失败案例包。
  现有比较预览、快照保存和回放入口已接入共享组件；失败案例包已有库接口和本地示例，尚未形成新的面板操作入口。
  两轮历史试验已结束；本次只做整合验证，不重开该探索方向。
- `codex/greedy-research-toy-20260921`：原有离线教学页面和中英文入口说明已保存为 `aaaf3f0` 并整合。
  已有三个教学案例、逐步回放、实例编辑与导入导出；未测量“两分钟理解”的教学目标，不能把它作为已验证效果。
- `codex/dashboard-ux-repair-20260913`：`5fcecda` 已是本地 `main` 的祖先；修复成果不再需要独立开发分支。
- `codex/fast-r2-verification`：`e1d865b` 已是本地 `main` 的祖先；生产后端和验证成果保留，旧实验不重跑。
  原工作目录残留已移入主检出的本地存档，旧环境停用；现用环境位于主检出的 `.venv`。
- `codex/integrate-remaining-20260916`：`83867b1` 已是本地 `main` 的祖先，原整合工作已完成。
- `main`：整合开始时为 `3de1200`，领先远端 `981fc59` 五个提交；这五个提交随本次整合一并交付。

## 已有历史与证据

远端刷新后，十五条已删除开发分支的本地远端引用已清除；其提交均已包含在本地 `main` 中。
涉及 acceleration-production、acceleration-verification、associations-split、dashboard-ux-pr、development-closeout、instance-family-validation、integrate-remaining、local-review、prepare-review、quality-statistics-split、r4-l5-dual、reduce-research-config-copies、release-v0.3.0、research-status-closeout 和 rust-structure 各条历史。

五条 `codex/evidence/*` 分支保存冻结的研究数据，属于结果来源，不能当作待合并的运行代码。
本次保留这五条数据分支，继续支持现有报告中的固定提交引用；开发分支的回收不删除研究证据。

旧暂存记录 `023ad024` 只包含早期研究计划和索引修改，已有当前 R2/R3 计划及报告承接；保留于恢复包，不覆盖现有材料。

七个旧验收克隆另存为 `legacy-checkout-01` 至 `07` 的文件压缩包及完整 Git 恢复包。
前三个对应本地审查的验收快照；临时提交 `afcb865` 中的审查代码与测试文件和当前主线完全一致。
第四、第五个是仅有 `value.py` 的引用传输测试仓库，保留为夹具，不混入生产源码。
最后两个是同一个 `0709c32` 冻结证据的本地检出副本，远端原始证据保持不变。

## 本地材料与恢复

整合前的完整 Git 历史保存在本地 `results/workspace_consolidation_20260923/before-integration.bundle`。
该目录同时保存各附加工作目录的压缩副本，以及主目录原有设计草图和日志。
源码、未提交材料和运行结果已打包回读；可重建的缓存、构建目录和虚拟环境未纳入压缩副本。
这些副本属于本机恢复材料，不等同于远端备份，也不随本文件公开发布。

各原目录的 3,061 个结果及输出文件已迁入主检出的 `results/archived_worktrees/consolidation-20260923-*/`。
组件试验的两个同名目录，以及 Rust 小规模、稀疏输入对比的两个同名目录，也已放到主检出的 `results/`，原报告链接继续可用。
面板已保存的比较、研究视图和任务记录一并迁入主检出；没有重新生成正式实验结果。

## 验收结果与使用

- 默认检查：675 项中 671 项通过、4 项跳过；57 个源码文件的静态类型检查通过。
  跳过项为一个可选 OR-Tools 检查、两个需明确启动的 CUDA 硬件检查，以及一个 POSIX 平台测试。
- Rust 专项：24 项全部通过，无跳过；主检出环境已安装对应的原生扩展 0.3.0。
- 独立行为审查：66 项 Python 测试通过，无跳过；另核对缺失实例、篡改结果和非法回放输入的拒绝行为。
- 教学页面：穷举核对 1,536 个小实例及全部贪心前缀，三个教学案例与 Python 贪心及穷举结果一致。
- 固定入口使用核查：21 次只读 HTTP 请求通过，首页、配置页、研究页、工作台及实际引用资源均从主检出加载。
  两个设计草图通过脚本语法、内联数据和依赖边界核查；本次没有重做真实浏览器交互验收。

检查日志保存在本地 `results/workspace_consolidation_20260923/default-check.log` 和 `rust-check.log`。
在主检出目录中可使用现有环境启动面板：

```powershell
.\.venv\Scripts\python.exe run_project.py dashboard
```

离线教学入口为 `examples/greedy-playground/index.html`；两个可视化设计器仍是草图，不会提交正式实验。
本次整合到此足够；本页列出的未完成能力不作为本次发布已经实现的功能。

## 整合收尾补查（2026-09-24）

7 份使用与复验文档仍有失效的工作树入口，本次改为从仓库根目录使用当前 `.venv`。
本地审查示例改用仍可解析的固定提交，避免引用已删除的开发分支；5 处历史环境叙述保留。
过期暂存 `023ad024` 的内容已有后续版本承接，已从暂存列表移除；原整合恢复包仍保留它。

所报权限异常路径实际为 13 个。逐个检查后，删除了 6 个空探针目录。
另外 7 个结果目录并非空目录，共有 57 个文件，包含实例轨迹、汇总表和验证材料。
经用户确认恢复这些目录的访问权限，文件全部保留，没有递归删除研究结果。
这些目录不再阻断文件检索；隔离账户访问用户级 Git 忽略文件的警告属于另一项环境限制。

复核包括当前解释器与工具帮助入口、固定提交解析、小域猜想搜索及独立验证。
默认检查完成：675 项中 671 项通过、4 项跳过，57 个源码文件的静态类型检查通过。
退出状态为 0；日志保存为 `results/workspace_consolidation_20260923/cleanup-default-check.log`。
没有重跑正式实验，也没有重新执行历史性能测量或付费模型审查。
