[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "maxcover-structure-native"
version = "0.3.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "maxcover_structure_native"
