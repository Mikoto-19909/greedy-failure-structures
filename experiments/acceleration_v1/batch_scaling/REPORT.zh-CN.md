# 大批量 R2 独立验证进程数试验

日期：2026-09-08。状态：36 次预定测量全部完成，结论仅针对独立验证命令。

在完整 1,800 张原图上，三个快速后端选项中实测中位数最小的是 **4 个进程**：55.162 秒。快速后端单进程为 175.092 秒，原始 Python 四进程为 106.747 秒。

所选快速后端相对快速单进程为 3.174 倍，相对原始 Python 四进程为 1.935 倍。这里没有测量超过 4 个进程，也没有声称该选择适用于其他机器或任务分布。

## 固定样本与测量规则

- 复用本地 R2 证据归档的原图；来源记录指向提交 4a419f338d70068fa988fa97027734cdcda0a036。本轮未重新生成图或替换困难样本。
- 在九个 n,d 单元内分别取原始 repetition=0..9、0..39、0..199，形成 90、360、1,800 张图。任务标识、种子、预算、诊断成员及原图内容与归档一致。
- 三个批量的诊断图比例为 100%、80%、16%。较小批量不是完整批次的随机缩样；这是不同实际工作量的执行比较，不能把跨批量差异单独归因于图数。
- 每个批量固定比较 Numba 1/2/4 进程及 Python 4 进程，各重复 3 次，按预定轮换顺序执行。样本、方法、重复次数及停止规则在结果产生前写入 plan.json。
- 每次实际执行 validate_r2_budget_grid.py。主指标从创建子进程计时到成功退出，包含解释器及依赖导入、JIT 磁盘缓存载入、全新工作进程池、读取检查点、全部独立图/诊断验证和报告写入。
- 正式计时前只预编译一个已知答案的小例子。操作系统文件缓存和 JIT 磁盘缓存可能已热；进程每次新建。本结果不是首次安装/首次编译的冷启动测量。
- 输入副本准备不计入命令时间；未执行生产、bootstrap、图表或整个 R2 流水线，因此以下倍率不能写为项目端到端加速倍率。
- 预设上限：总计 2 小时、单命令 20 分钟、最多 4 个工作进程、6 GiB 保守进程内存估计和 2 GiB 输出。失败/超时保留日志，不替换样本。

## 三次重复的中位数与范围

### 90 张图：650 个预算结果，90 张含诊断

- 快速后端，1 进程：**15.918 s**，范围 15.839–16.302 s；吞吐 5.654 图/s。
- 快速后端，2 进程：**9.594 s**，范围 9.541–9.708 s；吞吐 9.381 图/s。
- 快速后端，4 进程：**6.188 s**，范围 6.186–6.301 s；吞吐 14.543 图/s。
- 原始 Python，4 进程：**21.965 s**，范围 21.679–22.098 s；吞吐 4.097 图/s。

### 360 张图：2600 个预算结果，288 张含诊断

- 快速后端，1 进程：**53.426 s**，范围 52.964–55.770 s；吞吐 6.738 图/s。
- 快速后端，2 进程：**30.064 s**，范围 29.903–30.536 s；吞吐 11.975 图/s。
- 快速后端，4 进程：**17.975 s**，范围 17.755–17.977 s；吞吐 20.028 图/s。
- 原始 Python，4 进程：**69.403 s**，范围 68.458–70.994 s；吞吐 5.187 图/s。

### 1800 张图：13000 个预算结果，288 张含诊断

- 快速后端，1 进程：**175.092 s**，范围 172.135–175.839 s；吞吐 10.280 图/s。
- 快速后端，2 进程：**95.698 s**，范围 95.695–96.112 s；吞吐 18.809 图/s。
- 快速后端，4 进程：**55.162 s**，范围 55.114–55.398 s；吞吐 32.631 图/s。
- 原始 Python，4 进程：**106.747 s**，范围 106.398–107.023 s；吞吐 16.862 图/s。

## 完成情况与解释边界

- 36 次命令均退出成功，每次 verification.json 的 passed 状态和全部预期图 ID 都得到核对；总共执行 27,000 次独立图验证。
- 结束时重新核对三份本地子集配置与固定方案一致；2,250 个复制图文件均与归档源文件逐字节一致。代码固定为 b17a22652982ff0e2d8a6a1201284b004256f6b1，前后确认无源码差异。
- 36 次命令总墙钟时间为 32.88 分钟。最大单进程峰值 114.8 MiB；最大保守进程内存估计 556.5 MiB。它不是操作系统整体峰值测量。
- 三次重复保留全部观测，不剔除慢值。机器共享负载、调度与热状态可能变化；报告范围与中位数，不声称进行了统计显著性验证。
- 此前 9 张图试验测量的是预热父进程中的生产加分析流程；本轮测量的是新进程启动的独立验证命令。计时边界不同，不将两轮绝对时间相除，也不把跨轮差异全部归因于批量。
- 此前 9 张图试验中的单进程优势不能直接用于大批任务。具体进程数应结合本轮同一批量的三个快速后端结果，而非仅参考 CPU 核心数。
- 环境：Python 3.12.14，Numba 0.67.0，NumPy 2.3.5，32 个逻辑 CPU；记录的电源方案为 电源方案 GUID: 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c  (高性能)。测试没有修改线程数或电源设置。

## 复现与原始证据

benchmark.py prepare 固定本地输入副本与计划；benchmark.py run 执行或继续未完成的预定命令；benchmark.py summarize 重核输入并汇总；report.py 生成本报告与 CSV。所有命令从工作树根目录使用 .venv/Scripts/python.exe 运行。

已完成的 run 会复用成功测量，不会自动覆盖。每个命令的实际日志、退出码、时间和完整 verification.json 保存在 runs/<case>/attempt*/；失败尝试也保留。
timings.csv 保存 36 条原始命令测量；summary.csv 保存 12 个配置的中位数、范围、吞吐和峰值；environment.json 与 plan.json 保存环境及预定规则。
per-graph base/diagnostic 时间在多个工作进程中可能重叠；它们的和仅作工作量构成参考，不能与并行总墙钟时间直接相加。
