[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "maxcover-profile-native"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "maxcover_profile_native"
