# Pre-split projection fixture

`projection_protocol4.pickle` was serialized by the original
`maxcover.benchmark_associations._RuntimeKInstanceProjection` at `ad13f19`,
before moving that class. It checks old-path loading through compatibility exports.
Fields are `("config", "case", 2, "instance", "uniform", 3)` in declaration order.
This tiny local test fixture contains no experiment results or external data.
