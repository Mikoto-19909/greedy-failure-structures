# Benchmark regression inputs

These configuration and CSV files are fixed inputs for runner, summarize,
record-identity, and compatibility tests. Preserve their values when comparing
changes; the fixture needs no checksum or source-digest ledger.
