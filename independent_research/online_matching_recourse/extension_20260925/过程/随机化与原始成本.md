# 随机化与原始成本：追加式过程记录

## 2026-09-25 第一条：读取和模型确认

已读取原三点累计超额报告。固定第一步最近点，同分靠左。采用每请求终生一次改派和原子批量更新。本文负责研究原六条分支中的第 5 条随机化与第 6 条原始成本，不改变第一步规则。

检查现有环境得到 SciPy 1.18.1。没有安装依赖。读取 karpathy-guidelines 技能后，采用一个独立脚本完成枚举、线性规划与结果保存。

随机化区分两种对手：未来在抽样结果不可见时选定；未来在第二步实际匹配可见后选定。先证明所有动作存在共同有限断点，避免用每个动作各自最坏值代替混合后的最坏值。

## 2026-09-25 第二条：解析推导

累计超额得到无随机化收益的证明。中间首配且 x≤b 时，向右动作逐点被保留支配。共同未来 z=a 已使向左动作不优于保留。另一半区间对称。端点首配已有零超额纯策略。

原始成本得到任意前缀的三个闭式候选值。无约束第三步最坏点在 x 或 y。已固定首请求后的最坏点在 a、y、c。两种目标的随机混合均可精确压缩到共同断点 {a,b,c,x,y}。

在原服务点 (0,10,50)、前缀 (6,10) 上找到原始目标的严格随机收益：确定性 62，保留和向左改派各半得到 60。构造对手在 z=0、6 上以 2/3、1/3 混合，给出同值下界。累计超额在该例仍以纯保留最优，值为 8。

## 2026-09-25 第三条：首次执行与原始结果

新增 random_raw.py 后，使用 D:\test area\sci\.venv-medical\Scripts\python.exe 单进程执行。第一次运行即通过所有断言，未进行失败重试。

结果目录为 output/random_raw_20260925T052605840780Z/。主文件 verification.json 保存 845 个前缀的成本、策略概率、对手概率和断点。共解 1690 个线性规划。另检查 8820 个区间内部点。最大原始对偶残差为 3.552713678800501e-15，main 内 CPU 时间为 5.078125 秒。

累计超额的随机值全部等于确定性值。原始成本有 36 个前缀获得严格随机改善。最大网格改善为 1，出现在服务点 (0,2,6)、前缀 (4,2)，值从 10 变为 9。原始成本与累计超额的纯动作选择有 12 次不同；该数字包含同分差异，不能作为严格偏好冲突数。

写出报告/随机化与原始成本.md，包含连续证明、可复现实例、一般原始成本公式与线性规划。结果采用浮点求解并检查原始对偶约束；手算严格改善例额外使用 Fraction 验算上下界。
