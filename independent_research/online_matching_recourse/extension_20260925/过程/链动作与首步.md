# 链动作与首步过程记录

## 2026-09-25：模型与方法

负责用户新增目标的第 1、2 分支。先读旧三点报告及 `matching.py`。采用 karpathy-guidelines 技能约束实现规模。本轮新增一个标准库脚本，没有改变原有实验文件。

单链模型固定首步最近点，第三步排除旧请求二环加新请求独立空点的动作。原子模型自由首步必须保留第二步全部六种匹配，旧证明基于首点最近的支配删减不适用。

实现 Fraction 有理数分段程序。对固定前缀先枚举最终成本下包络的断点。对自由首步再把二维成本等式直线投影到第二请求轴，处理候选最坏场景和最优动作之间的交点。独立排列筛选器未复用路径生成函数。

## 首次核验

首次运行的 1,690 次前缀模型检查和 21 个完整首步博弈全部通过。CPU 30.84375 秒。结果保存在 `output/chain_first_20260925T132523/verification.json`。

最初的区别例为服务点 (0,3,6)、前缀 (2,5/2)，链值为 2、原子值为 1。后续细分有理网格找到更强例：服务点 (0,2,6)、前缀 (5/4,2)。原子模型保留值 3/2；单链提前改派值 5/2，所有保留动作至少为 3。

自由首步例采用服务点 (0,3,6)、首请求 7/4。首占 0、3、6 的完整最坏累计超额分别为 1、5/2、11/2。另行给出首占 0 的上下界手算证明。较远首点的严格收益不是同分规则造成的。

## 增补核验与输出修正

增加两个非半整数首请求，首步博弈数增至 23。增加 48 个离网格有理数前缀检查，并持久化严格时机反转例。

增补运行的数学断言通过，结果写入 `output/chain_first_20260925T132918/verification.json`。末尾控制台摘要直接序列化 Fraction 时出错，进程退出码为 1。修正摘要序列化后重新运行完整入口。旧输出保留，最终验收使用修正后的新目录。

## 最终入口复跑

最终输出为 `output/chain_first_20260925T133017/verification.json`。进程退出码 0。1,690 次前缀模型检查、23 个首步博弈、48 次离网格有理数前缀检查通过。CPU 33.1875 秒。

报告给出四动作穷尽、连续第三请求断点覆盖、自由首步二维分段投影与下包络的证明。主例数字与持久输出逐项核对。用户本人审阅和新颖性判断均未在本分支完成。
