# 六分支独立复核

主助手完成拆分与四请求计算。链与自由首步、随机化与原始目标、文献各由不同助手负责。下列复核发生在本轮交付前，没有代替用户本人审阅。

## 链动作与自由首步

另一名助手只读核查二维分段方法与实现。等成本竖直线、等式直线交点、与z边界交点、第二阶段成本交点都纳入y分段。各动作最坏分数的交点再次细分，覆盖min与max切换。首步超额加入位置正确，选择顺序为min_p max_y min_A max_z。

主助手发现例子的上界说明错误：不能声称第三步始终保持旧分配。正确证明是：若禁止交换是唯一最优，直接动作比它多付3/2；否则采用另一个合法最优。已修正文稿，数值和算法不变。自由首例的D(y)分段及y=9/2下界由另一助手独立核对。

## 随机化与原始成本

另一名助手逐点核对共同未来证明和闭式最大值。未导入原程序，独立重算845条保存记录的成本和原/对偶证书，确认36个严格改善前缀。最大证书残差为3.552713678800501e-15。

用有理数核查完整六动作、四断点的60上下界。各半策略的四个期望成本为[60,60,56,16]。对手取0、6的概率2/3、1/3时，六动作期望成本为[60,100,60,90,100,90]。因此60证书不依赖浮点容差。

## 更多请求与拆分

另一名助手确认请求0出发的匹配链必须终止于未到达服务点。最优取等强制该链单调，因此首请求服务点符号逐步改变，n−1必要性成立。逐到达重算给出原子模型的充分性。

19到23及83/79/95与保存轨迹一致。链长贡献10、价格增量0受到同输入、同预算比较支持。独立验证器采用全部单射筛选，未导入策略程序。

## 四请求有限博弈

第二实现按(请求坐标、服务编号、计数)排序成规范状态。精确参照采用排序请求配服务子集，链从空位逆向生长。它不导入原solver。

六个在线博弈值4/2/0与三条已知未来对照2/2/0均一致。全部36个保存阶段的匹配、计数、精确参照、递推等式与动作最优性通过。该核验只覆盖明确定义的有限坐标集合。

## 文献

新增三篇PDF已检查文件头、指纹与页数，关键算法页已目视核对。指定目标全文尚未取得。检索失败记录不用于断言论文尚未公开。主助手再次打开官方录用目录与两个arXiv原始页面核对题名和模型摘要。

## 连续四请求补充复核

另一个助手独立核对保持首步最近服务点的在线取整规则。各阶段实际成本与精确最优均为坐标L1变化的1-Lipschitz函数，第一步超额为零，余下误差权重给12上界。网格包含服务点且最大间隙h时得到12h。

细网格4局以规范请求状态、排序配子集参照和逆向空位链复算，9/2数值一致。16个证据阶段全部通过。失败的180秒尝试与成功的179.03125秒记录分开保留。
